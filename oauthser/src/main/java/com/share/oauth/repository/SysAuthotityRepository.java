package com.share.oauth.repository;


import com.share.oauth.domain.SysAuthority;
import com.share.oauth.repository.support.WiselyRepository;

public interface SysAuthotityRepository extends WiselyRepository<SysAuthority,Long> {
}
